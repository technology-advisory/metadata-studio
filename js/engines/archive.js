(function(){
  const M=window.MetaStudio,U=M.utils;
  const MAX_FILES=1500,MAX_UNCOMPRESSED=512*1024*1024;
  function zipList(bytes){const z=new M.ZipLite(bytes),f=[];let total=0;for(const e of z.entries.slice(0,MAX_FILES)){const safe=U.safePath(e.name);total+=e.size;if(!safe)f.push(U.finding('Ruta insegura',e.name,'Archivo comprimido',true,{source:'ZIP',risk:'critico',path:e.name}));if(!e.name.endsWith('/'))f.push(U.finding('Elemento interno',e.name,'Archivo comprimido',false,{source:'ZIP',risk:'info',path:e.name,kind:'technical'}));}if(z.entries.length>MAX_FILES)f.push(U.finding('Límite de elementos',`${z.entries.length} entradas; se muestran ${MAX_FILES}`,'Archivo comprimido',false,{source:'ZIP',risk:'medio'}));if(total>MAX_UNCOMPRESSED)f.push(U.finding('Tamaño descomprimido elevado',U.humanSize(total),'Archivo comprimido',false,{source:'ZIP',risk:'alto'}));return {z,f,total};}
  async function inspectZipChildren(z,f,depth=0){if(depth>=2)return;let scanned=0,total=0;for(const e of z.entries){if(e.name.endsWith('/')||!U.safePath(e.name))continue;if(++scanned>120)break;if((total+=e.size)>128*1024*1024)break;const eng=M.engineFor&&M.engineFor(e.name,true);if(!eng||eng===M.archive||eng===M.universal)continue;try{const b=await z.read(e.name);const child=await eng.analyze(b,{name:e.name,type:''});for(const x of child){x.path=e.name+(x.path?' → '+x.path:'');x.source=(x.source||x.category)+' · dentro de ZIP';f.push(x);}}catch(err){f.push(U.finding('No se pudo inspeccionar',e.name,'Archivo comprimido',false,{source:'ZIP',risk:'info',path:e.name,kind:'technical'}));}}}
  function tarList(bytes){const f=[];let p=0,count=0,total=0;while(p+512<=bytes.length&&count<MAX_FILES){const h=bytes.subarray(p,p+512);if(h.every(x=>x===0))break;const name=U.text(h.subarray(0,100)).replace(/\0.*$/,'');const sizeTxt=U.text(h.subarray(124,136)).replace(/\0.*$/,'').trim();const size=parseInt(sizeTxt,8)||0;const uname=U.text(h.subarray(265,297)).replace(/\0.*$/,'').trim(),gname=U.text(h.subarray(297,329)).replace(/\0.*$/,'').trim();if(!U.safePath(name))f.push(U.finding('Ruta insegura',name,'Archivo comprimido',true,{source:'TAR',risk:'critico',path:name}));else f.push(U.finding('Elemento interno',name,'Archivo comprimido',false,{source:'TAR',risk:'info',path:name,kind:'technical'}));if(uname)f.push(U.finding('Usuario TAR',uname,'Identidad',true,{source:'TAR header',risk:'alto',path:name}));if(gname)f.push(U.finding('Grupo TAR',gname,'Identidad',true,{source:'TAR header',risk:'alto',path:name}));total+=size;count++;p+=512+Math.ceil(size/512)*512;}return {f,total,count};}
  async function gunzip(bytes){if(typeof DecompressionStream==='undefined')throw new Error('Este navegador no soporta descompresión gzip local.');const ds=new DecompressionStream('gzip');return new Uint8Array(await new Response(new Blob([bytes]).stream().pipeThrough(ds)).arrayBuffer());}
  M.archive={supports(ext){return ['zip','7z','tar','gz','tgz','tar.gz','tar.bz2','tar.xz'].includes(ext);},async analyze(bytes,file){const ext=U.ext(file.name);if(ext==='zip'){const q=zipList(bytes);await inspectZipChildren(q.z,q.f);return q.f;}if(ext==='tar')return tarList(bytes).f;if(['gz','tgz','tar.gz'].includes(ext)){const inner=await gunzip(bytes);const f=[U.finding('Capa gzip',`${U.humanSize(bytes.length)} → ${U.humanSize(inner.length)}`,'Archivo comprimido',false,{source:'GZIP',risk:'info',kind:'technical'})];if(ext!=='gz'||U.text(inner.subarray(257,262))==='ustar')f.push(...tarList(inner).f);return f;}if(ext==='7z')return [U.finding('Contenedor 7Z','Formato detectado. Inspección interna profunda no disponible sin decodificador 7Z/WASM local.','Archivo comprimido',false,{source:'7Z',risk:'info',kind:'technical'})];if(['tar.bz2','tar.xz'].includes(ext))return [U.finding('Contenedor comprimido',`Formato ${ext.toUpperCase()} detectado. Inspección profunda pendiente de decodificador local.`, 'Archivo comprimido',false,{source:ext.toUpperCase(),risk:'info',kind:'technical'})];return[];},async clean(bytes,selected,file){const ext=U.ext(file.name);if(ext==='zip'){const {z}=zipList(bytes);for(const e of z.entries){if(!U.safePath(e.name))throw new Error('El ZIP contiene rutas inseguras y no se reempaqueta automáticamente.');}const byPath=new Map();for(const f of selected){if(!f.path)continue;const top=f.path.split(' → ')[0];if(!byPath.has(top))byPath.set(top,[]);byPath.get(top).push(f);}for(const [path,items] of byPath){const eng=M.engineFor&&M.engineFor(path,true);if(!eng||eng===M.archive||eng===M.universal)continue;try{const original=await z.read(path);const local=items.map(x=>({...x,path:''}));const cleaned=await eng.clean(original,local,{name:path,type:''});z.set(path,cleaned);}catch(_){}}return await z.build();}if(ext==='tar'){const out=bytes.slice();let p=0;while(p+512<=out.length){const h=out.subarray(p,p+512);if(h.every(x=>x===0))break;const size=parseInt(U.text(h.subarray(124,136)).replace(/\0.*$/,'').trim(),8)||0;if(selected.some(f=>f.source==='TAR header')){h.fill(0,265,329);for(let i=148;i<156;i++)h[i]=32;let sum=0;for(const x of h)sum+=x;const oct=sum.toString(8).padStart(6,'0')+'\0 ';h.set(U.utf8(oct),148);}p+=512+Math.ceil(size/512)*512;}return out;}throw new Error('Este contenedor se inspecciona, pero no se sanea automáticamente en esta versión.');}};
})();

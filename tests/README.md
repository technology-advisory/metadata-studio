# Tests de seguridad v0.9.5

Ejecutar desde la raíz del proyecto:

```bash
node tests/security.test.js
```

Cubre límites previos de tamaño, coherencia de firma/extensión, validación estructural ZIP, CRC/lectura almacenada y comportamiento fail-closed de PDF complejo.
